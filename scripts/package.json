{
  "name": "scripts",
  "version": "1.0.0",
  "description": "The deployment scripts",
  "main": "src/index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf build ;  mkdir build",
    "build": "yarn clean; ts-node-dev src/build.ts",
    "deploy": "ts-node-dev src/index.ts",
    "activate": "ts-node-dev src/activate.ts"
  },
  "dependencies": {
    "@taquito/signer": "^12.0.0",
    "@taquito/taquito": "^12.0.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}

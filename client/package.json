{
  "name": "client",
  "version": "1.0.0",
  "description": "A client for fetching data from db and publishing it to the tezos blockchain",
  "main": "src/index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "ts-node echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/mysql": "^2.15.21",
    "@types/node": "^17.0.23",
    "dotenv": "^16.0.0",
    "mysql": "^2.18.1",
    "node-cron": "^3.0.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}
